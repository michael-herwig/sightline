import { describe, expect, it } from "vitest";

// ponytail: placeholder so `task test` has something to run before Phase 3
// splits the planer into tested modules. Delete once real unit tests land.
describe("vitest wiring", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
